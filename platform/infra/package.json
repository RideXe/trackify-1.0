{
  "name": "@trackify/infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK app for the Trackify platform",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --testTimeout=15000",
    "synth": "cdk synth --quiet --no-notices -c env=dev -c account=111111111111 -c alertEmail=alerts@example.com",
    "cdk": "cdk"
  },
  "dependencies": {
    "aws-cdk-lib": "2.269.0",
    "constructs": "10.8.1"
  },
  "devDependencies": {
    "aws-cdk": "2.1141.0",
    "tsx": "4.23.13"
  }
}
